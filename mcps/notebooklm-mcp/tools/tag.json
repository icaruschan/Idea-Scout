{
  "name": "tag",
  "description": "Manage notebook tags and find relevant notebooks by tag matching.\n\nActions:\n- add: Add tags to a notebook for smart selection\n- remove: Remove tags from a notebook\n- list: List all tagged notebooks with their tags\n- select: Find notebooks relevant to a query using tag matching",
  "inputSchema": {
    "additionalProperties": false,
    "properties": {
      "action": {
        "type": "string",
        "description": "Operation to perform (add, remove, list, select)"
      },
      "notebook_id": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Notebook UUID (required for add, remove)"
      },
      "tags": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Comma-separated tags (required for add, remove; e.g. \"ai,research,llm\")"
      },
      "notebook_title": {
        "default": "",
        "type": "string",
        "description": "Optional display title (for add)"
      },
      "query": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Search query (required for select; e.g. \"ai mcp\" or \"ai,mcp\")"
      }
    },
    "required": [
      "action"
    ],
    "type": "object"
  }
}