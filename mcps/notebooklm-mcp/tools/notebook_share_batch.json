{
  "name": "notebook_share_batch",
  "description": "Invite multiple collaborators in a single request.",
  "inputSchema": {
    "additionalProperties": false,
    "properties": {
      "notebook_id": {
        "type": "string",
        "description": "Notebook UUID"
      },
      "recipients": {
        "items": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        },
        "type": "array",
        "description": "List of dicts, each with 'email' (str) and optional 'role' (str).\n        Role defaults to 'viewer'. Example: [{\"email\": \"a@b.com\", \"role\": \"editor\"}]"
      },
      "confirm": {
        "default": false,
        "type": "boolean",
        "description": "Must be True after user approval"
      }
    },
    "required": [
      "notebook_id",
      "recipients"
    ],
    "type": "object"
  }
}