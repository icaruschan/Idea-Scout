{
  "name": "download_artifact",
  "description": "Download any NotebookLM artifact to a file.\n\nUnified download tool replacing 9 separate download tools.\nSupports all artifact types: audio, video, report, mind_map, slide_deck,\ninfographic, data_table, quiz, flashcards.",
  "inputSchema": {
    "additionalProperties": false,
    "properties": {
      "notebook_id": {
        "type": "string",
        "description": "Notebook UUID"
      },
      "artifact_type": {
        "type": "string",
        "description": "Type of artifact to download:\n- audio: Audio Overview (MP4/MP3)\n- video: Video Overview (MP4)\n- report: Report (Markdown)\n- mind_map: Mind Map (JSON)\n- slide_deck: Slide Deck (PDF or PPTX)\n- infographic: Infographic (PNG)\n- data_table: Data Table (CSV)\n- quiz: Quiz (json|markdown|html)\n- flashcards: Flashcards (json|markdown|html)"
      },
      "output_path": {
        "type": "string",
        "description": "Path to save the file"
      },
      "artifact_id": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Optional specific artifact ID (uses latest if not provided)"
      },
      "output_format": {
        "default": "json",
        "type": "string",
        "description": "For quiz/flashcards only: json|markdown|html (default: json)"
      },
      "slide_deck_format": {
        "default": "pdf",
        "type": "string",
        "description": "For slide_deck only: pdf (default) or pptx"
      },
      "wait": {
        "default": true,
        "type": "boolean",
        "description": "If True, poll while NotebookLM is still generating or propagating the artifact."
      },
      "wait_timeout": {
        "default": 180.0,
        "type": "number",
        "description": "Maximum seconds to wait when wait=True."
      },
      "poll_interval": {
        "default": 5.0,
        "type": "number",
        "description": "Seconds between readiness checks."
      }
    },
    "required": [
      "notebook_id",
      "artifact_type",
      "output_path"
    ],
    "type": "object"
  }
}