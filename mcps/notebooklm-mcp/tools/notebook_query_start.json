{
  "name": "notebook_query_start",
  "description": "Start a notebook query asynchronously for large notebooks that may timeout.\n\nUse this instead of notebook_query when querying notebooks with many sources\n(50+) where the response may take longer than 60 seconds. Returns immediately\nwith a query_id. Poll notebook_query_status with the query_id to get the result.\n\nWorkflow: notebook_query_start -> poll notebook_query_status until completed.",
  "inputSchema": {
    "additionalProperties": false,
    "properties": {
      "notebook_id": {
        "type": "string",
        "description": "Notebook UUID"
      },
      "query": {
        "type": "string",
        "description": "Question to ask"
      },
      "source_ids": {
        "anyOf": [
          {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Source IDs to query (default: all)"
      },
      "conversation_id": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "For follow-up questions"
      },
      "timeout": {
        "anyOf": [
          {
            "type": "number"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Request timeout in seconds (default: from env NOTEBOOKLM_QUERY_TIMEOUT or 120.0)"
      }
    },
    "required": [
      "notebook_id",
      "query"
    ],
    "type": "object"
  }
}