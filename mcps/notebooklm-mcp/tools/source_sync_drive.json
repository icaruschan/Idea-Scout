{
  "name": "source_sync_drive",
  "description": "Sync Drive sources with latest content. Requires confirm=True.\n\nCall source_list_drive first to identify stale sources.",
  "inputSchema": {
    "additionalProperties": false,
    "properties": {
      "source_ids": {
        "items": {
          "type": "string"
        },
        "type": "array",
        "description": "Source UUIDs to sync"
      },
      "confirm": {
        "default": false,
        "type": "boolean",
        "description": "Must be True after user approval"
      }
    },
    "required": [
      "source_ids"
    ],
    "type": "object"
  }
}