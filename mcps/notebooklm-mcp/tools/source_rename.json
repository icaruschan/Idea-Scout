{
  "name": "source_rename",
  "description": "Rename a source in a notebook.",
  "inputSchema": {
    "additionalProperties": false,
    "properties": {
      "notebook_id": {
        "type": "string",
        "description": "Notebook UUID containing the source"
      },
      "source_id": {
        "type": "string",
        "description": "Source UUID to rename"
      },
      "new_title": {
        "type": "string",
        "description": "New display title for the source"
      }
    },
    "required": [
      "notebook_id",
      "source_id",
      "new_title"
    ],
    "type": "object"
  }
}