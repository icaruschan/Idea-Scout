{
  "name": "source_get_content",
  "description": "Get raw text content of a source (no AI processing).\n\nReturns the original indexed text from PDFs, web pages, pasted text,\nor YouTube transcripts. Much faster than notebook_query for content export.",
  "inputSchema": {
    "additionalProperties": false,
    "properties": {
      "source_id": {
        "type": "string",
        "description": "Source UUID"
      },
      "wait": {
        "default": true,
        "type": "boolean",
        "description": "If True, poll while NotebookLM is still indexing the source."
      },
      "wait_timeout": {
        "default": 120.0,
        "type": "number",
        "description": "Maximum seconds to wait when wait=True."
      },
      "poll_interval": {
        "default": 3.0,
        "type": "number",
        "description": "Seconds between readiness checks."
      }
    },
    "required": [
      "source_id"
    ],
    "type": "object"
  }
}